# dashboard-setup

Command: `/dashboard-setup`

Use this when you want a visual dashboard for the workspace.

It helps create or refresh a simple dashboard so the system is easier to scan.

Good after the workspace has real files in it.

Actual runtime file: `.claude/skills/dashboard-setup/SKILL.md`
