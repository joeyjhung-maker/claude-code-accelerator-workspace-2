# vault-setup

Command: `/vault-setup`

Use this first.

It interviews you, updates the Script (`CLAUDE.md`), creates your first client file, starts today's daily note, confirms the link map, and points you to the skills already inside the workspace.

The important part is not the interview itself. The important part is that Claude now knows where the main pieces live.

Actual runtime file: `.claude/skills/vault-setup/SKILL.md`
