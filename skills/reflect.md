# reflect

Command: `/reflect`

Use this at the end of a session.

It helps decide what is worth keeping, what should stay as ordinary work, and what belongs in memory files like `winners/`, `losers/`, `brand/`, `clients/`, or `workflows/`.

This is how the workspace gets smarter without turning into clutter.

Actual runtime file: `.claude/skills/reflect/SKILL.md`
