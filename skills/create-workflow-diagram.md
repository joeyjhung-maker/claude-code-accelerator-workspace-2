# create-workflow-diagram

Command: `/create-workflow-diagram`

Use this when a process needs a picture.

It turns a workflow into a diagram you can teach from, inspect, or improve.

This skill may need Node the first time it runs. You do not need it for basic setup.

Actual runtime file: `.claude/skills/create-workflow-diagram/SKILL.md`
