# 2026-09-01

## Made today

**Reviewed the NHB Pro success-stories post** (Jenny Hansen Lane, Dan Redgewell, Mrs Meta Ads, Mr
Media Buyer). Flagged: banned word "leverage" in the Mrs Meta Ads section, a redundant sentence in
Dan's section, a confusing mentor-name mapping in the "commonalities" recap, and a "Sir Charles
Kirkland" vs. "Charles" naming inconsistency. Joey confirmed it's a keeper post, not something to
edit — the real value was the Mr Media Buyer story, which triggered the rest of the session.

**Built the first automation in this workspace: Flexxable weekly storm-refill.** Mr Media Buyer's
story ("wakes up to a big portion of his work already done") made Joey name the actual problem —
he's running every stage of the creative pipeline manually and it's the ceiling on taking another
client. Built a scheduled Routine (`trig_01DpvYK3QLiyFRJ9zhLQC2ey`, Tuesdays 11:00 UTC / ~7am ET,
fresh session each fire) that runs `/storm` for Flexxable and drafts (never locks) proposed briefs
on the 3-5 strongest seeds. Deliberately stops short of full automation: `/brief`'s own skill file
has a hard rule ("never pick the seed/angle for him — propose, he decides"), and `/produce` can't
run headless anyway (mariobot writes or no one writes, no Claude-direct fallback). Documented in
`workflows/2026-09-01-flexxable-weekly-storm-refill.md`. First run already fired since today is a
Tuesday — worth checking `clients/flexxable/seeds/` and `clients/flexxable/briefs/` for output.

**Diagnosed the static-image text-quality problem — unresolved, blocked on Joey.** Joey's been
finishing Claude Code's product-stack/text renders in ChatGPT by hand because the text comes out
garbled. Traced it: nothing here calls Claude for images — `scripts/run_image.py` hits KIE using
`google/nano-banana` / `nano-banana-2`, and there's an existing banked failure mode
(`kie-render-reference.md`) for exactly this — garbled/duplicated text on longer blocks, plus a
separate gotcha where it invents a fake book cover with placeholder text. gpt-image (same model
family ChatGPT uses) is already listed as available on the *same* KIE key in this workspace's own
docs (`kie-render-reference.md`, `tool-registry.md`) but nobody's wired the actual model slug into
`run_image.py` yet — it only hardcodes the two nano-banana variants. Proposed fix has two parts:
(1) look up KIE's real gpt-image model id and wire it in as a `--model` option, test head-to-head
against nano-banana-2; (2) for the "reuse the same base image across 3 new headline variants/week"
plan Joey wants to run — stop baking headline/proof text into the AI render at all, render the base
visual clean once, then overlay text with Canva (MCP tools already connected in this session, unused
anywhere in the workspace so far) so new variants are a template text-swap, not a re-render. Neither
part is built — this needs Joey's real IAA Statics images to test against, and they haven't reached
this environment (see open loops).

## Decisions made

- Weekly (not nightly) cadence for the storm-refill automation, tied to the account-read rhythm.
- Scope = seeds + proposed (unconfirmed) brief drafts, not full produce — respects the brief skill's
  "propose, he decides" rule.
- The NHB Pro post ships as-is; the copy fixes flagged were informational, not requested edits.

## Files changed

- `workflows/2026-09-01-flexxable-weekly-storm-refill.md` (new, committed + pushed)
- Trigger `trig_01DpvYK3QLiyFRJ9zhLQC2ey` created (not a file, but load-bearing — see workflow doc)
- `daily/2026-09-01.md` (this file)

## Open loops

- **Blocking:** Joey's IAA Statics images have not reached this session. This is a remote/cloud
  Claude Code container — it cannot see his local Mac's Downloads folder. Checked every possible
  landing spot (`/mnt/attach`, `/mnt/user-data/working`, home dirs) — all empty. He needs to either
  drag the actual files into the chat, or run this specific thread from a local Claude Code session
  that has real filesystem access to his Mac.
- gpt-image's exact KIE model slug is unconfirmed — needs a lookup against KIE's docs/model list
  before wiring into `run_image.py`.
- No Canva template exists yet for the book/product-stack reuse plan — needs one of Joey's base
  renders to build against.
- First fire of the storm-refill trigger happened today (2026-09-01, since it landed on a Tuesday)
  — nobody has checked its output yet.
- Joey hasn't been asked yet what's worth promoting into memory from this session (per the standing
  end-of-session rule) — the automation pattern itself may be worth a `workflows/` promotion once
  it's proven over a few weeks (already flagged in the workflow doc).

## Exact next step

Get Joey's IAA Statics images into the session (drag into chat, or switch to a local session), then:
1. Look at the actual renders to see which specific images are garbled/weak.
2. Look up KIE's real gpt-image model slug and test one problem image through it via
   `scripts/run_image.py --model <slug>`, compared against the current nano-banana-2 output.
3. If gpt-image doesn't fully solve it, build the Canva split (clean base render + text overlay
   template) using one of the base images as the first worked example.
4. Separately: check `clients/flexxable/seeds/` and `clients/flexxable/briefs/` for today's
   storm-refill trigger output and report back to Joey.

## Resume prompt

Paste this into a fresh Claude Code session:

```text
Continue from daily/2026-09-01.md. Two open threads for Flexxable:

1. Image quality: Joey's IAA Statics (product-stack/text renders) look worse out of this workspace's
   pipeline than when he finishes them by hand in ChatGPT. Root cause already diagnosed: this repo's
   scripts/run_image.py only calls KIE's nano-banana/nano-banana-2 (Gemini), which has a banked
   text-garbling failure mode (.claude/skills/creative-strategy-system/knowledge/kie-render-reference.md).
   gpt-image is available on the same KIE key per that same doc and tool-registry.md, but its exact
   model slug was never looked up or wired in. First get Joey's actual images into this session (drag
   them into chat — this may be a remote container with no access to his local Downloads folder,
   confirm before assuming otherwise), then look up KIE's gpt-image model id, test it against
   nano-banana-2 on a real garbled example, and if headline/proof text still needs to be swappable
   without re-rendering, build a Canva template (MCP tools already connected) that overlays text on a
   clean base render instead of baking it into the AI image.

2. Check output: a weekly automation (trig_01DpvYK3QLiyFRJ9zhLQC2ey, documented in
   workflows/2026-09-01-flexxable-weekly-storm-refill.md) fired for the first time today. Check
   clients/flexxable/seeds/ and clients/flexxable/briefs/ for what it produced and report to Joey —
   he hasn't seen the output yet.

Read clients/flexxable/client.md and the two files above before doing anything.
```
