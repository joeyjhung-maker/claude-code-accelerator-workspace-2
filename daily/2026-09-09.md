# 2026-09-09

## Git workflow fix: no more `git add -A`

Joey is now running Claude Code and Codex at the same time against the same GitHub repo
(two-machine setup, shared remote). Flagged the risk that a blanket `git add -A` at close-out
could sweep up the other tool's uncommitted work into a Claude commit.

Corrected `daily-git-commit-on-closeout` in memory: Claude now stages and commits only the
specific files it created or touched in its own session, by name — never `git add -A`,
`git add .`, or `git commit -a`. Anything else sitting in `git status` is left for the other
tool to commit itself.

No copy or creative work this session.

## Codex close-out — Dan Skool posts

Finalized and banked two B2B Leads Lab posts from the Codex session: the LinkedIn-first “warm
before the pitch” philosophy teaser and the lumpy-mail fishing swipe/poll. Joey's corrections
promoted three lessons into the Dan judge and voice memory: add the emotional bridge to practical
advice; teach the transferable philosophy before teasing a tool; and preserve each section's
persuasive function when adapting a structural swipe.

Task Observer recorded two actioned `produce-skool` corrections in
`skills/observations.md` (Observations 10–11).

Close-out respected concurrent ownership: only Codex-owned files/hunks were staged. Claude Code's
registered-office, carousel, Genesis-authorisation, and sentence-formatting changes were left
untouched.
